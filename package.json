{
  "name": "cathy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "preview": "vite preview",
    "dev": "vite --host 0.0.0.0 --port 3001",
    "build": "export NODE_OPTIONS=--max_old_space_size=5120 && tsc && vite build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.0.0"
  }
}